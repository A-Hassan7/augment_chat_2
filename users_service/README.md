- Register users with the application (handle authentication)
- Create matrix account for user
- Create bridges for user when requested

schema:

- users
  - id
  - username
  - matrix username

matrix_room_memberships:
  - 