services:
  redis:
    image: redis/redis-stack-server:latest
    container_name: redis
    restart: always
    ports:
      - 6379:6379
      - 8001:8001
    command: redis-server --requirepass password
    volumes:
      - redis:/data

volumes:
  redis:
